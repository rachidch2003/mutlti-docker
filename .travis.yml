sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t rachidcherkaoui/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true stephengrider/react-test npm test

after_success:
  - docker build -t rachidcherkaoui/multi-client ./client
  - docker build -t rachidcherkaoui/multi-nginx ./nginx
  - docker build -t rachidcherkaoui/multi-server ./server
  - docker build -t stephengrider/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push rachidcherkaoui/multi-client
  - docker push rachidcherkaoui/multi-nginx
  - docker push rachidcherkaoui/multi-server
  - docker push rachidcherkaoui/multi-worker
